<?php
/**
 * ekiline functions and definitions.
 *
 * @link https://developer.wordpress.org/themes/basics/theme-functions/
 *
 * @package ekiline
 */

/**
 * Estilos CSS // Stylesheets CSS
 * Para usar la hoja de estilos del tema habilitar el siguiente código || For custom CSS enable this code.
 * Puedes cambiar a tema minificado: style.min.css || You can switch to minified archive: style.min.css
 */
// function child_theme_enqueue_styles() {
    // $parent_style = 'ekiline-style';
    // wp_enqueue_style( $parent_style, get_template_directory_uri() . '/style.css' );
    // wp_enqueue_style( 'ekiline-child-style', get_stylesheet_directory_uri() . '/style.css', array( $parent_style ), wp_get_theme()->get('Version') );
// }
// add_action( 'wp_enqueue_scripts', 'child_theme_enqueue_styles' );